import Foundation

enum TestFixtures {
    /// Shared dummy password for unit tests. Not a real credential.
    static let dummyPassword = "test-fixture-password" // gitleaks:allow
    /// Shared dummy token for unit tests. Not a real credential.
    static let dummyToken = "test-fixture-token" // gitleaks:allow
}
