apiVersion: v1
kind: Secret
metadata:
  name: {{ include "openclaw.name" . }}-secrets
  labels:
    {{- include "openclaw.labels" . | nindent 4 }}
type: Opaque
stringData:
  OPENCLAW_GATEWAY_TOKEN: {{ .Values.gateway.token | quote }}
